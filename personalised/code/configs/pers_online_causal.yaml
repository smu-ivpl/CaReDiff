# Root-level top config for ONLINE personalized (perfrdiff rewrite-weight), mirror of
# pers_offline_causal.yaml: re-hosts the personalized_online defaults at the global
# package so --config-name overrides compose correctly. Non-invasive.
defaults:
  - /personalized_online/data/perfrdiff_rewrite_weight@data
  - /personalized_online/trainer/perfrdiff_rewrite_weight@trainer
  - /shared/logger: none
  - /shared/path@path
  - _self_

seed: 1234
logger_level: INFO
task: online
stage: ???
data_dir: ???
resume: false
run_id: ${generate_id:}
resume_id: ""
num_gts: 10
